project AoC is
    for Source_Dirs use (".", "src");
    for Object_Dir use "obj";
    for Main use ("adavent_of_code.adb");
    for Exec_Dir use ".";

    package Compiler is
        for Switches ("Ada") use ("-O0", "-g", "-gnat2022", "-gnatwa");
    end Compiler;
end AoC;
